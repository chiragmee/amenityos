# Archive

Early draft specs written before the current documentation set (`docs/00`–`18`
plus root `AGENTS.md`). Superseded, not wrong — kept for reference only. Do
not implement against these; see `docs/00-documentation-index.md` for the
current source of truth.
